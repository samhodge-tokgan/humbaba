# Python deps for the model->ONNX pipeline (CPU).
#
# Depth Anything 3 is installed separately and WITHOUT its own deps
# (`pip install -e da3-src --no-deps`), because a full install tries to build
# xformers, which has no macOS-arm64 wheel. tools/_da3_compat.py stubs the heavy
# optional modules (open3d/moviepy/evo/... ) that the ONNX depth path never runs.
# The list below is the curated subset DA3 actually needs to load + export depth.
--extra-index-url https://download.pytorch.org/whl/cpu
numpy<2
addict
einops
safetensors
huggingface_hub
omegaconf
pillow
imageio
typer
scipy
matplotlib
opencv-python-headless<4.10
# ONNX side
onnx>=1.15
onnxruntime>=1.17
onnxconverter-common>=1.14
# dynamo exporter (dynamic-resolution ONNX)
onnxscript>=0.7

